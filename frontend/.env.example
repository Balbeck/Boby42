# Optionnel : par défaut chatApi.js utilise un chemin relatif (/chat), relayé
# vers le backend par le proxy Vite (voir vite.config.js). Ne renseigner cette
# variable que pour cibler un backend qui n'est pas sur localhost:8420.
# VITE_API_URL=http://localhost:8420
